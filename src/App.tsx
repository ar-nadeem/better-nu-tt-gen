import { useState } from "react";
import { useTheme } from "next-themes";

function App() {
  const { theme, setTheme } = useTheme();

  return (
    <>
      <div></div>
    </>
  );
}

export default App;
